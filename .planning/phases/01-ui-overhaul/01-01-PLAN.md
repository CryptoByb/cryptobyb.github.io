---
phase: 01-ui-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - StableCoin Blacklist Checker/index.html
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-04

must_haves:
  truths:
    - "Page has a minimal dashboard-style header: CryptoByB logo left-aligned, tagline below logo text, no hero copy"
    - "Four tabs render in a pill segmented control: Blacklist Check, Token Approvals, TX Tracer, Batch Check"
    - "Active tab has solid #4f8ef7 background with dark (#07080f) text; inactive tabs have transparent background with muted text"
    - "Background is #07080f with subtle blue grid overlay; all cards/inputs use #0d0e17 surface with #1a1b2e border"
    - "All text uses JetBrains Mono; no Inter except optional body copy"
    - "Existing JS functionality is 100% intact — tab switching, blacklist check, tracer, batch all work"
  artifacts:
    - path: "StableCoin Blacklist Checker/index.html"
      provides: "Single self-contained HTML file with all CSS/JS inline"
      contains: "nav-seg"
    - path: "StableCoin Blacklist Checker/index.html"
      provides: "Segmented tab control"
      contains: "tab-approvals"
  key_links:
    - from: ".nav-seg button[data-tab]"
      to: "#tab-{name}"
      via: "JS click handler using data-tab attribute"
      pattern: "tab-\\w+"
    - from: ".nav-seg .seg-tab.active"
      to: "background:#4f8ef7"
      via: "CSS .seg-tab.active rule"
      pattern: "seg-tab.*active"
---

<objective>
Redesign the page chrome — header and tab navigation — to establish the CryptoByB premium dark aesthetic. Replace the current border-bottom nav with a pill segmented control. Add the Token Approvals tab (shell only). Harden CSS variables to match the locked design spec.

Purpose: First visual impression. Header and tab are the frame for every interaction. Getting these right makes the tool feel like a product instead of a demo.
Output: Updated index.html with new header HTML, segmented tab CSS, four-tab nav structure. All four tab content panels preserved. All existing JS intact.
</objective>

<execution_context>
@/c/Users/basit/OneDrive/Desktop/CLAUDE/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/c/Users/basit/OneDrive/Desktop/CLAUDE/StableCoin Blacklist Checker/.planning/ROADMAP.md
@/c/Users/basit/OneDrive/Desktop/CLAUDE/StableCoin Blacklist Checker/.planning/phases/01-ui-overhaul/01-CONTEXT.md

<interfaces>
<!-- Current HTML structure the executor is modifying. Extracted from index.html. -->

Current header (lines 183–191):
```html
<header class="hdr">
  <a class="logo" href="/">
    <div class="logo-icon">B</div>
    <div>
      <div class="logo-name">CryptoByB</div>
      <div class="logo-tag">ON-CHAIN SECURITY</div>
    </div>
  </a>
</header>
```

Current nav (lines 193–197) — 3 tabs, class="nav", buttons class="nav-tab":
```html
<nav class="nav">
  <button class="nav-tab active" data-tab="blacklist">Blacklist Check</button>
  <button class="nav-tab" data-tab="tracer">TX Tracer</button>
  <button class="nav-tab" data-tab="batch">Batch Check</button>
</nav>
```

Current tab JS event listener (lines 388–395) uses `.nav-tab` selector and `data-tab` attribute:
```js
document.querySelectorAll('.nav-tab').forEach(btn=>{
  btn.addEventListener('click',()=>{
    document.querySelectorAll('.nav-tab').forEach(b=>b.classList.remove('active'));
    document.querySelectorAll('.tab').forEach(t=>t.classList.remove('active'));
    btn.classList.add('active');
    document.getElementById('tab-'+btn.dataset.tab).classList.add('active');
  });
});
```

Current CSS variables (lines 12–21):
```css
:root{
  --bg:#07080f; --surface:#0d0e1a; --surface2:#13141f; --surface3:#1a1b2a;
  --border:rgba(79,142,247,0.07); --border2:rgba(79,142,247,0.14); --border3:rgba(79,142,247,0.28);
  --text:#e2e8f0; --muted:#64748b; --muted2:#94a3b8;
  --accent:#4f8ef7; --accent-dim:rgba(79,142,247,0.1);
  --green:#22c55e; --green-dim:rgba(34,197,94,0.1);
  --red:#ef4444; --red-dim:rgba(239,68,68,0.1);
  --amber:#f59e0b; --amber-dim:rgba(245,158,11,0.1);
  --mono:'JetBrains Mono',monospace; --sans:'Inter',sans-serif;
  --r:10px;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CSS variables and header styles</name>
  <read_first>
    - StableCoin Blacklist Checker/index.html (full file — read before any edit)
    - StableCoin Blacklist Checker/.planning/phases/01-ui-overhaul/01-CONTEXT.md
  </read_first>
  <files>StableCoin Blacklist Checker/index.html</files>
  <action>
Update the `<style>` block CSS with these exact changes:

1. Update `:root` — harden border variable to solid hex (not transparent blue):
```css
:root{
  --bg:#07080f;
  --surface:#0d0e17;
  --surface2:#111220;
  --surface3:#1a1b2e;
  --border:#1a1b2e;
  --border-glow:rgba(79,142,247,0.18);
  --text:#e2e8f0;
  --muted:#4a5568;
  --muted2:#718096;
  --accent:#4f8ef7;
  --accent-dim:rgba(79,142,247,0.08);
  --green:#22c55e;
  --green-dim:rgba(34,197,94,0.08);
  --red:#ef4444;
  --red-dim:rgba(239,68,68,0.08);
  --amber:#f59e0b;
  --amber-dim:rgba(245,158,11,0.08);
  --mono:'JetBrains Mono',monospace;
  --r:8px;
}
```

2. Update `body` — use `var(--mono)` as primary font, remove `var(--sans)` fallback:
```css
body{background:var(--bg);color:var(--text);font-family:var(--mono);min-height:100vh;overflow-x:hidden;line-height:1.5}
```

3. Update `.wrap` max-width to 860px, keep padding:
```css
.wrap{position:relative;z-index:1;max-width:860px;margin:0 auto;padding:36px 24px 80px}
```

4. Replace all `.hdr`, `.logo*` styles with this cleaner version:
```css
/* Header */
.hdr{display:flex;align-items:center;justify-content:space-between;margin-bottom:40px;padding-bottom:24px;border-bottom:1px solid var(--border)}
.logo{display:flex;align-items:center;gap:10px;text-decoration:none}
.logo-mark{width:32px;height:32px;background:var(--accent);border-radius:6px;display:flex;align-items:center;justify-content:center;font-family:var(--mono);font-size:14px;font-weight:700;color:#07080f;flex-shrink:0}
.logo-text .logo-name{font-family:var(--mono);font-size:15px;font-weight:600;color:#fff;letter-spacing:-0.01em;line-height:1.2}
.logo-text .logo-tag{font-family:var(--mono);font-size:9px;color:var(--muted2);letter-spacing:0.12em;text-transform:uppercase;line-height:1}
```

5. Replace `.nav` and `.nav-tab` CSS with segmented control:
```css
/* Segmented tabs */
.nav-seg{display:flex;gap:2px;background:var(--surface);border:1px solid var(--border);border-radius:10px;padding:3px;margin-bottom:28px}
.seg-tab{flex:1;font-family:var(--mono);font-size:11px;font-weight:500;color:var(--muted2);background:none;border:none;border-radius:7px;padding:9px 10px;cursor:pointer;transition:color .12s,background .12s;letter-spacing:.02em;text-align:center;white-space:nowrap}
.seg-tab:hover{color:var(--text)}
.seg-tab.active{background:var(--accent);color:#07080f;font-weight:600}
```

Keep all other existing CSS rules unchanged.
  </action>
  <verify>
    Open index.html in browser. Verify: background is very dark (#07080f), header has a bottom border line, logo mark is a solid blue square with "B". No visual regressions in overall layout.
  </verify>
  <acceptance_criteria>
    - `grep -c 'nav-seg' 'StableCoin Blacklist Checker/index.html'` returns at least 2 (CSS definition + HTML usage)
    - `grep -c 'logo-mark' 'StableCoin Blacklist Checker/index.html'` returns at least 2
    - `grep -c '#0d0e17' 'StableCoin Blacklist Checker/index.html'` returns at least 1 (surface variable updated)
    - CSS contains `.seg-tab.active{background:var(--accent);color:#07080f`
    - CSS contains `--border:#1a1b2e`
  </acceptance_criteria>
  <done>CSS variables match locked spec. Header styles defined. Segmented tab CSS defined. No JS touched.</done>
</task>

<task type="auto">
  <name>Task 2: Update HTML — header markup, four-tab nav, Approvals tab shell</name>
  <read_first>
    - StableCoin Blacklist Checker/index.html (full file — confirm Task 1 changes landed before editing)
  </read_first>
  <files>StableCoin Blacklist Checker/index.html</files>
  <action>
Make the following HTML changes. DO NOT touch any `<script>` tags.

1. Replace the `<header class="hdr">` block with:
```html
<header class="hdr">
  <a class="logo" href="/">
    <div class="logo-mark">B</div>
    <div class="logo-text">
      <div class="logo-name">CryptoByB</div>
      <div class="logo-tag">On-Chain Security</div>
    </div>
  </a>
</header>
```

2. Replace `<nav class="nav">` block with a four-tab segmented control. The new nav uses class `nav-seg` and buttons use class `seg-tab`. The `data-tab` attributes and the `active` class on Blacklist must be preserved exactly for the existing JS to work:
```html
<nav class="nav-seg">
  <button class="seg-tab active" data-tab="blacklist">Blacklist Check</button>
  <button class="seg-tab" data-tab="approvals">Token Approvals</button>
  <button class="seg-tab" data-tab="tracer">TX Tracer</button>
  <button class="seg-tab" data-tab="batch">Batch Check</button>
</nav>
```

3. Add a new `#tab-approvals` div immediately after `</div>` closing the `#tab-blacklist` div and before the `<!-- TX TRACER TAB -->` comment. The approvals tab is a placeholder shell for Phase 2:
```html
<!-- ══ TOKEN APPROVALS TAB ══════════════════════════════════════════════════ -->
<div class="tab" id="tab-approvals">
  <div style="text-align:center;padding:60px 20px;font-family:var(--mono);font-size:13px;color:var(--muted);border:1px dashed var(--border);border-radius:var(--r)">
    Token Approvals scanner — coming soon
  </div>
</div>
```

4. Update the JS tab event listener selector: change `.nav-tab` to `.seg-tab` in BOTH querySelectorAll calls in the tab nav JS block. The block currently reads:
```js
document.querySelectorAll('.nav-tab').forEach(btn=>{
  btn.addEventListener('click',()=>{
    document.querySelectorAll('.nav-tab').forEach(b=>b.classList.remove('active'));
```
Change both `.nav-tab` occurrences to `.seg-tab`:
```js
document.querySelectorAll('.seg-tab').forEach(btn=>{
  btn.addEventListener('click',()=>{
    document.querySelectorAll('.seg-tab').forEach(b=>b.classList.remove('active'));
```
Leave the rest of the block (classList.add, getElementById) unchanged.
  </action>
  <verify>
    Open index.html in browser. Verify:
    - Header shows blue square "B" mark, "CryptoByB" text, "On-Chain Security" subtag
    - Four tabs render in a pill row: Blacklist Check (active/blue), Token Approvals, TX Tracer, Batch Check
    - Clicking each tab switches content (including Approvals which shows the "coming soon" placeholder)
    - Blacklist Check functionality still works (enter an address, click Check Blacklist)
    - TX Tracer and Batch Check tabs still work
  </verify>
  <acceptance_criteria>
    - `grep -c 'nav-seg' 'StableCoin Blacklist Checker/index.html'` returns at least 2 (CSS class + HTML nav element)
    - `grep -c 'tab-approvals' 'StableCoin Blacklist Checker/index.html'` returns at least 2 (data-tab + id)
    - `grep -c 'seg-tab' 'StableCoin Blacklist Checker/index.html'` returns at least 6 (CSS + 4 buttons + JS selectors)
    - `grep -c 'logo-mark' 'StableCoin Blacklist Checker/index.html'` returns at least 2 (CSS + HTML)
    - File contains `class="seg-tab active" data-tab="blacklist"`
    - File contains `querySelectorAll('.seg-tab')` (not `.nav-tab`)
    - File does NOT contain `class="nav-tab"` or `class="nav"` (old classes removed)
  </acceptance_criteria>
  <done>Header and four-tab nav are in place. Tab switching works including new Approvals shell. JS selector updated to match new class name. Single index.html file, all inline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → JS validation | Address field input crosses into validation logic |
| JS → External RPC endpoints | All RPC calls go to external public nodes over HTTPS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | URL params (?address=, ?token=) | mitigate | Existing: address is validated via isEvm/isTron before use; token is checked against 'USDT'/'USDC' allowlist — no change needed in this plan |
| T-01-02 | Information Disclosure | sessionStorage history | accept | Low risk: session-only, no PII beyond wallet addresses user explicitly entered, no backend |
| T-01-03 | Spoofing | External RPC responses | accept | Read-only eth_call — attacker controlling an RPC gets false blacklist result, user is warned to use tool for reference only |
</threat_model>

<verification>
After both tasks complete:

1. `grep -c 'nav-seg' 'StableCoin Blacklist Checker/index.html'` — expect >= 2
2. `grep -c 'seg-tab' 'StableCoin Blacklist Checker/index.html'` — expect >= 6
3. `grep -c 'tab-approvals' 'StableCoin Blacklist Checker/index.html'` — expect >= 2
4. `grep -c 'logo-mark' 'StableCoin Blacklist Checker/index.html'` — expect >= 2
5. `grep -v '^//' 'StableCoin Blacklist Checker/index.html' | grep -c 'nav-tab'` — expect 0 (old class gone)
6. File is valid HTML — opens without console errors
7. All four tabs switch correctly in browser
8. Blacklist check, TX Tracer, and Batch Check remain fully functional
</verification>

<success_criteria>
- Four-tab segmented control renders with active state visually distinct (solid blue fill vs transparent)
- Header is compact: logo mark + CryptoByB text + tagline, left-aligned, with a bottom border separator
- CSS variables match locked spec: --bg:#07080f, --surface:#0d0e17, --border:#1a1b2e, --accent:#4f8ef7
- Token Approvals tab exists as a placeholder shell
- Zero JS functionality regressions — all existing tabs and features work
</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-overhaul/01-01-SUMMARY.md` with:
- What changed (header, nav, CSS variables)
- New class names introduced (nav-seg, seg-tab, logo-mark, logo-text)
- Removed class names (nav, nav-tab, logo-icon)
- Any deviations from plan
</output>
